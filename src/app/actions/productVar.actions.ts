
export interface ProductVarType {
  _id: string
  productID: string
  size: string
  quantity: number
}

export async function getProductVar(productID: string, formData: FormData) {
  try {

  } catch {}
}